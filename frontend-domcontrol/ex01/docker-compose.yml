version: '3'

services:
  nginx:
    build:
      context: ./
      dockerfile: nginx.dockerfile
    restart: always
    volumes:
      - ./srcs:/usr/share/nginx/html:ro
    ports:
      - 8080:8080
    env_file:
      - ./.env
