const Person = {
  name: 'jaham',
  age: 26,
  attr: 'member'
};

function makeEmployee(name, age, sabun) {
  return {
    name,
    age,
    sabun,
  }
}

const child = new Person;
child.something = 1;

class Student {
 constructor(
   this
 )
}

const hi = new Student;

